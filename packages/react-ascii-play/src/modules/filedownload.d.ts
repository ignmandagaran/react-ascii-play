import { AsciiBuffer } from "../types";

export function saveSourceAsFile(
  src: AsciiBuffer[],
  filename: string
): void;
export function saveBlobAsFile(
  blob: AsciiBuffer[],
  filename: string
): void;
